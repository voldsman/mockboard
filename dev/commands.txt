QUICK NOTES:
---
Change git branch name: git branch -m <name>

---
Clean & package maven: mvn clean package
Clean & package maven skip tests: mvn clean package -DskipTests

---
Set maven version:
set: mvn versions:set -DnewVersion=<NEW_VERSION>
apply: mvn versions:commit
revert: mvn versions:revert

---
Dev Actuator access (dev profile):
http://127.0.0.1:8000/actuator

---
H2 console (dev profile):
http://localhost:8000/h2

---
JSON Stringify site for quick test of converting mock "body" json to json string:
https://jsonformatter.org/json-stringify-online

---
Intellij H2 DB:
path: jdbc:h2:file:../data/mockboard_dev;MODE=MySQL;AUTO_SERVER=TRUE

---
Docker compose commands (run from project ROOT):
> docker compose up -d
> docker compose up -d --build
> docker compose down -v
> docker compose logs -f mockboard_dev