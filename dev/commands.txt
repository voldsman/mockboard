---
Clean & package maven: mvn clean package
Clean & package maven skip tests: mvn clean package -DskipTests

---
Set maven version (root): mvn versions:set -DnewVersion=<NEW_VERSION>
apply: mvn versions:commit
revert: mvn versions:revert

---
Change git branch name: git branch -m <name>

---
Run custom compose file: docker compose -f <compose-file.yml> up -d
<root> docker compose -f dev/mongo-compose.yml up -d
---